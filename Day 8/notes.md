To find the index of a variable in a list, we can use the .index method.

For example:

alphabet = ['a', 'b', 'c', 'd']

letter_index = alphabet.index('a')

or

word = "hello"

for letter in word:
letter_index = alphabet.index(letter)
